// Code generated by Wire. DO NOT EDIT.

//go:generate go run -mod=mod github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package migration

import (
	"admin/internal/infrastructure/persistence"
	"admin/internal/support/config"
	"context"
)

// Injectors from wire.go:

func InitializeMigrator(ctx context.Context, configPath string) (*Migrator, error) {
	configConfig, err := config.New(configPath)
	if err != nil {
		return nil, err
	}
	db, err := persistence.NewDB(configConfig)
	if err != nil {
		return nil, err
	}
	repository := persistence.NewRepositoryFromDB(db)
	migrator := NewMigrator(configConfig, db, repository)
	return migrator, nil
}
