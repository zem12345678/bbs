service:
  name: search-service
  grpcPort: 9106

app:
  name: search-service

nacos:
  addr: "127.0.0.1"
  port: 8848
  namespaceId: ""
  dataId: "search-service.yaml"
  groupId: "DEFAULT_GROUP"

log:
  filename: logs/search-service.log
  maxSize: 100
  maxBackups: 7
  maxAge: 30
  level: info
  stdout: true

elasticsearch:
  # Override in production with comma-separated BBS_SEARCH_ELASTICSEARCH_ADDRESSES.
  addresses:
    - http://127.0.0.1:9200
  indices:
    articles: bbs_articles
    topics: bbs_topics

es:
  url:
    - http://127.0.0.1:9200
  enable_debug_logger: false
  indices:
    articles: bbs_articles
    topics: bbs_topics

kafka:
  # Override in production with comma-separated BBS_SEARCH_KAFKA_BROKERS.
  brokers:
    - 127.0.0.1:9092
  articleTopic: article.events
  commentTopic: comment.events
  reactionTopic: reaction.events
  articleGroupId: bbs-search-indexer
  commentGroupId: bbs-search-comment-counter
  reactionGroupId: bbs-search-reaction-counter

grpc:
  server:
    port: 9106
    etcdAddr:
      - "127.0.0.1:2379"
    serviceName: "search-service"
    timeout: 10s
  client:
    timeout: 10s
    tag: "search"
    etcdAddr:
      - "127.0.0.1:2379"
    serverName: "search-service"
    secure: false

trace:
  grpcEndpoint: 127.0.0.1:4317
  serviceName: search-service
  version: local
  env: local
