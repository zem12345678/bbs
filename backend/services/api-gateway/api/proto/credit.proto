syntax = "proto3";

package bbs.credit.v1;

option go_package = "api-gateway/internal/clients/pb/creditpb;creditpb";

service CreditService {
  rpc GetBalance(GetBalanceRequest) returns (BalanceResponse);
  rpc ListLedger(ListLedgerRequest) returns (ListLedgerResponse);
}

message Balance {
  int64 user_id = 1;
  int64 total = 2;
  int64 updated_at = 3;
}

message LedgerEntry {
  int64 id = 1;
  int64 user_id = 2;
  int64 delta = 3;
  int64 balance_after = 4;
  string reason = 5;
  string description = 6;
  string source_event_id = 7;
  string source_type = 8;
  int64 source_id = 9;
  int64 created_at = 10;
}

message GetBalanceRequest {
  int64 user_id = 1;
}

message BalanceResponse {
  Balance balance = 1;
}

message ListLedgerRequest {
  int64 user_id = 1;
  int32 limit = 2;
  int32 offset = 3;
}

message ListLedgerResponse {
  repeated LedgerEntry items = 1;
  int64 total = 2;
  Balance balance = 3;
}
