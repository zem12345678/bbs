# 2026-07-28 工作日志

## C 端前端 UI 重构（参照社区客户端参考图 + 知乎，蓝白主题）

对 `frontend/`（React 19 + Vite + 纯 CSS）做了多轮 UI/布局重构，未动后端与 API。

### 关键改动
- **去掉顶栏 Header**：`App.jsx` 不再渲染 `Header.jsx`（文件保留未删，仅无引用）；handleLogout/navigateToPage/handleSearch 已清理。
- **左栏（PageColumns.jsx LeftColumn）**：品牌区（Bird 图标+站名，siteConfig 传入）→ 竖向主导航（广场/话题/圈子/聊天室/求助/资源/商城/会员 + 分隔线 + 主页/提醒/收藏/点赞/钱包/设置/更多，"更多"在最底部）→ 广场页分类筛选面板 → 底部用户区（登录后：头像+@username 单行，跳 /user/profile；未登录：蓝色登录按钮）。auth 通过 `readStoredAuth()` 读取，避免 30 处路由传参。
- **右栏（RightColumn）**：搜索框 + 热门话题 + 热门聊天频道 + 热门资源（后两者为静态数据 hotChatChannels/hotResources，用户明确要求不调 API）+ 社区分类。
- **视觉风格**：白底（--page: #fff）、三栏内 panel 平铺（`.left-column > .panel, .right-column > .panel, .feed > .panel` 透明无边框）、feed 左右竖线 + 帖子间 border-bottom 分隔线流、零阴影、小字号（正文 15px）。
- **Composer**：紧凑模式（头像+单行"说说您的新鲜事..."），聚焦/有内容时展开（标题/标签/分类/预览），发布按钮蓝色小胶囊。
- **PostCard**：action-row 主操作（赞/评论/收藏/详情，gap 52px）+ 次要操作（分享/举报/编辑/归档）包在 `.action-row__more` 右推。
- **设计 token**：`:root` 扩充圆角/阴影/--field/--blue-ring/--topbar-h/--col-left(232px)/--col-right(300px)/--grid-max(1280px) 等。

### 构建注意（重要）
- `npm run build` 会因 safe-delete shim 无法清空 `dist/` 而失败，需先 `mv dist /tmp/xxx` 再构建。
- Bash 工具每次调用都是新 shell，cd 不保留，npm 命令必须带 `cd /d/projects/bbs/frontend &&`。
- 构建预算：入口 JS ≤320KB（当前 ~290KB）。
- 契约测试 `npm test`：235 个全部通过。
- playwright 截图方案未落地（venv 安装多次未成功），视觉验证靠用户 dev server（127.0.0.1:8850，用户自己开着）。

### 未完成/后续
- Header.jsx 已成死代码，可后续删除（含通知轮询逻辑；左栏"提醒"导航未带未读徽章）。
- 热门聊天频道/热门资源目前是静态假数据，后续可接真实 API。

## 日夜间模式（同日追加）
- 新增 `src/lib/theme.js`：currentTheme/applyTheme/toggleTheme/subscribeTheme，localStorage key `bbs:theme`，变化派发自定义事件。
- `index.html` 内联初始化脚本防闪烁：先读 localStorage，否则跟随 `prefers-color-scheme`。
- 切换按钮在 LeftColumn 品牌区右侧（`.theme-toggle`，Sun/Moon 图标）。
- CSS：`[data-theme="dark"]` 变量覆盖（深蓝黑 --page:#0d1520、--panel:#141d2a、蓝色提亮 #3d9bff），styles.css 尾部有"夜间模式补丁"区块处理输入框/markdown/category-chip 等硬编码；三栏主干样式已收敛为变量。
- 深层页面（商城/dashboard 等）仍有硬编码色，暗色下可能不完美，后续按需迭代。
